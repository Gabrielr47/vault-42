import { AssetImageUrlPipe } from './asset-image-url.pipe';

describe('AssetImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
